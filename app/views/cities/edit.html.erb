<h1>Editing City</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>

    <%= link_to 'Show', edit_country_city_path(@country, @city) %> |
    <%= link_to 'Back', country_cities_path(@country) %>
  </div>
</div>
